// Range collections are also called "structures" by the IIIF Presentation api
import Collection from 'ampersand-collection'
import Range from './range'

export default Collection.extend({

  model: Range

})
